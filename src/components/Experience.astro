---
import { experiences } from "../data/experience";
---

<section id="experience" class="max-w-5xl mx-auto px-6 py-32">

  <h2 class="text-4xl font-bold mb-12">
    Experience
  </h2>

  <div class="space-y-12">

    {
      experiences.map((experience) => (
        <div class="border border-gray-800 rounded-2xl p-8">

          <!-- Company -->
          <div class="mb-10">

            <h3 class="text-3xl font-bold">
              {experience.company}
            </h3>

            <p class="text-gray-500 mt-2">
              {experience.location}
            </p>

          </div>

          <!-- Timeline -->
          <div class="relative ml-3 space-y-10">

            <!-- Better Timeline Line -->
            <div
              class="absolute left-0 top-0 bottom-0 w-px bg-gray-800"
            ></div>

            {
              experience.roles.map((role) => (
                <div class="relative pl-10">

                  <!-- Timeline Dot -->
                  <div
                    class="absolute -left-[7px] top-3 w-4 h-4 rounded-full bg-white border-4 border-black"
                  ></div>

                  <!-- Expandable Card -->
                  <details
                    class="group border border-gray-800 rounded-2xl p-6"
                  >

                    <summary
                      class="list-none cursor-pointer flex items-center justify-between"
                    >

                      <div>

                        <h4 class="text-2xl font-semibold">
                          {role.role}
                        </h4>

                        <p class="text-gray-400 mt-1">
                          {role.type}
                        </p>

                        <p class="text-sm text-gray-500 mt-2">
                          {role.duration}
                        </p>

                      </div>

                      <!-- Arrow Icon -->
                      <svg
                        xmlns="http://www.w3.org/2000/svg"
                        class="w-5 h-5 text-gray-400 transition-transform duration-300 group-open:rotate-180"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke="currentColor"
                        stroke-width="2"
                      >
                        <path
                          stroke-linecap="round"
                          stroke-linejoin="round"
                          d="M19 9l-7 7-7-7"
                        />
                      </svg>

                    </summary>

                    <!-- Points -->
                    <ul class="space-y-4 text-gray-300 mt-8">

                      {
                        role.points.map((point) => (
                          <li class="flex gap-3 items-start">

                            <div
                              class="w-2 h-2 rounded-full bg-gray-500 mt-2"
                            ></div>

                            <span>
                              {point}
                            </span>

                          </li>
                        ))
                      }

                    </ul>

                  </details>

                </div>
              ))
            }

          </div>

        </div>
      ))
    }

  </div>

</section>